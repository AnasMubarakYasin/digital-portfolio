import type { ParentComponent } from "solid-js";

export interface ErrUnauthcProps { }

const Unauthenticate: ParentComponent<ErrUnauthcProps> = function ({ children }) {
  return (
    <div>Unauthenticate</div>
  );
};

export default Unauthenticate;
